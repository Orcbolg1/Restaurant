package RestaurantSystem.component.item;


import RestaurantSystem.service.ItemService;

public class AddItem extends javax.swing.JFrame {

    private final ItemService itemService;

    /**
     * Creates new form InsertItem
     */
    public AddItem(){

        //this.itemService = new ItemService();
    }

    /**
     * This method is called from within the constructor to initialize the form.
     * Functions:
     * add addButtonActionPerformed
     *     will composed of itemfield, itempriced and quanitity. Then create the new item
     * add backButtonActionPerformed
     *     goes back
     */


}
